if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/archer")
  add_subdirectory(archer)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Modules")
  add_subdirectory(Modules)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/multiplex")
  add_subdirectory(multiplex)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/omptest")
  add_subdirectory(omptest)
endif()
