include(LLVM-Build)

# `Demangle', `Support' and `TableGen' libraries are added on the top-level
# CMakeLists.txt

if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ABI")
  add_subdirectory(ABI)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/IR")
  add_subdirectory(IR)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/FuzzMutate")
  add_subdirectory(FuzzMutate)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/FileCheck")
  add_subdirectory(FileCheck)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/InterfaceStub")
  add_subdirectory(InterfaceStub)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/IRPrinter")
  add_subdirectory(IRPrinter)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/IRReader")
  add_subdirectory(IRReader)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/CAS")
  add_subdirectory(CAS)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/CGData")
  add_subdirectory(CGData)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/CodeGen")
  add_subdirectory(CodeGen)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/CodeGenTypes")
  add_subdirectory(CodeGenTypes)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/BinaryFormat")
  add_subdirectory(BinaryFormat)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Bitcode")
  add_subdirectory(Bitcode)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Bitstream")
  add_subdirectory(Bitstream)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/DWARFLinker")
  add_subdirectory(DWARFLinker)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Extensions")
  add_subdirectory(Extensions)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Frontend")
  add_subdirectory(Frontend)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Transforms")
  add_subdirectory(Transforms)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Linker")
  add_subdirectory(Linker)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Analysis")
  add_subdirectory(Analysis)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/DTLTO")
  add_subdirectory(DTLTO)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/LTO")
  add_subdirectory(LTO)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/MC")
  add_subdirectory(MC)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/MCA")
  add_subdirectory(MCA)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ObjCopy")
  add_subdirectory(ObjCopy)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Object")
  add_subdirectory(Object)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ObjectYAML")
  add_subdirectory(ObjectYAML)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Option")
  add_subdirectory(Option)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Remarks")
  add_subdirectory(Remarks)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Debuginfod")
  add_subdirectory(Debuginfod)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/DebugInfo")
  add_subdirectory(DebugInfo)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/DWARFCFIChecker")
  add_subdirectory(DWARFCFIChecker)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/DWP")
  add_subdirectory(DWP)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ExecutionEngine")
  add_subdirectory(ExecutionEngine)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Target")
  add_subdirectory(Target)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/SandboxIR")
  add_subdirectory(SandboxIR)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/AsmParser")
  add_subdirectory(AsmParser)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/LineEditor")
  add_subdirectory(LineEditor)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ProfileData")
  add_subdirectory(ProfileData)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Passes")
  add_subdirectory(Passes)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Plugins")
  add_subdirectory(Plugins)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/TargetParser")
  add_subdirectory(TargetParser)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/TextAPI")
  add_subdirectory(TextAPI)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Telemetry")
  add_subdirectory(Telemetry)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ToolDrivers")
  add_subdirectory(ToolDrivers)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/XRay")
  add_subdirectory(XRay)
endif()
if (LLVM_INCLUDE_TESTS)
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Testing")
  add_subdirectory(Testing)
endif()
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/WindowsDriver")
  add_subdirectory(WindowsDriver)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/WindowsManifest")
  add_subdirectory(WindowsManifest)
endif()

set(LLVMCONFIGLIBRARYDEPENDENCIESINC "${LLVM_BINARY_DIR}/tools/llvm-config/LibraryDependencies.inc")

# Special components which don't have any source attached but aggregate other
# components
add_llvm_component_group(all-targets LINK_COMPONENTS ${LLVM_TARGETS_TO_BUILD})
add_llvm_component_group(Engine)

# The native target may not be enabled when cross compiling
if(TARGET ${LLVM_NATIVE_ARCH})
  add_llvm_component_group(Native LINK_COMPONENTS ${LLVM_NATIVE_ARCH})
  add_llvm_component_group(NativeCodeGen LINK_COMPONENTS ${LLVM_NATIVE_ARCH}CodeGen)
else()
  add_llvm_component_group(Native)
  add_llvm_component_group(NativeCodeGen)
endif()

# Component post-processing
LLVMBuildResolveComponentsLink()
LLVMBuildGenerateCFragment(OUTPUT ${LLVMCONFIGLIBRARYDEPENDENCIESINC})
