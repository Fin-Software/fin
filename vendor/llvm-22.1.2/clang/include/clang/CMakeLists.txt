if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/AST")
  add_subdirectory(AST)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Basic")
  add_subdirectory(Basic)
endif()
if(CLANG_ENABLE_CIR)
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/CIR")
  add_subdirectory(CIR)
endif()
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Options")
  add_subdirectory(Options)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Parse")
  add_subdirectory(Parse)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Sema")
  add_subdirectory(Sema)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Serialization")
  add_subdirectory(Serialization)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/StaticAnalyzer/Checkers")
  add_subdirectory(StaticAnalyzer/Checkers)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Tooling/Syntax")
  add_subdirectory(Tooling/Syntax)
endif()
