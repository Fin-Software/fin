if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Atomic")
  add_subdirectory(Atomic)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Directive")
  add_subdirectory(Directive)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Driver")
  add_subdirectory(Driver)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/HLSL")
  add_subdirectory(HLSL)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/OpenACC")
  add_subdirectory(OpenACC)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/OpenMP")
  add_subdirectory(OpenMP)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Offloading")
  add_subdirectory(Offloading)
endif()
