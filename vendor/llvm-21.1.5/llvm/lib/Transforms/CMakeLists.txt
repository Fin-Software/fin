if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Utils")
  add_subdirectory(Utils)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Instrumentation")
  add_subdirectory(Instrumentation)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/AggressiveInstCombine")
  add_subdirectory(AggressiveInstCombine)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/InstCombine")
  add_subdirectory(InstCombine)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Scalar")
  add_subdirectory(Scalar)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/IPO")
  add_subdirectory(IPO)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Vectorize")
  add_subdirectory(Vectorize)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/ObjCARC")
  add_subdirectory(ObjCARC)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Coroutines")
  add_subdirectory(Coroutines)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/CFGuard")
  add_subdirectory(CFGuard)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/HipStdPar")
  add_subdirectory(HipStdPar)
endif()
