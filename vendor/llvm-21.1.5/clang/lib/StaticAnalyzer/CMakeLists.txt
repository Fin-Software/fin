if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Core")
  add_subdirectory(Core)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Checkers")
  add_subdirectory(Checkers)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Frontend")
  add_subdirectory(Frontend)
endif()
