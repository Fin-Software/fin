if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/IR")
  add_subdirectory(IR)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/OpenACC")
  add_subdirectory(OpenACC)
endif()
if(EXISTS  "${CMAKE_CURRENT_SOURCE_DIR}/Transforms")
  add_subdirectory(Transforms)
endif()
