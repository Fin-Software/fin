if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Headers")
  add_subdirectory(Headers)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Basic")
  add_subdirectory(Basic)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/APINotes")
  add_subdirectory(APINotes)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Lex")
  add_subdirectory(Lex)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Parse")
  add_subdirectory(Parse)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/AST")
  add_subdirectory(AST)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/ASTMatchers")
  add_subdirectory(ASTMatchers)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/CrossTU")
  add_subdirectory(CrossTU)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Sema")
  add_subdirectory(Sema)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/CodeGen")
  add_subdirectory(CodeGen)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Analysis")
  add_subdirectory(Analysis)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Edit")
  add_subdirectory(Edit)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/ExtractAPI")
  add_subdirectory(ExtractAPI)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Rewrite")
  add_subdirectory(Rewrite)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Driver")
  add_subdirectory(Driver)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Options")
  add_subdirectory(Options)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Serialization")
  add_subdirectory(Serialization)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Frontend")
  add_subdirectory(Frontend)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/FrontendTool")
  add_subdirectory(FrontendTool)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Tooling")
  add_subdirectory(Tooling)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/DependencyScanning")
  add_subdirectory(DependencyScanning)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/DirectoryWatcher")
  add_subdirectory(DirectoryWatcher)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Index")
  add_subdirectory(Index)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/IndexSerialization")
  add_subdirectory(IndexSerialization)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/InstallAPI")
  add_subdirectory(InstallAPI)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/StaticAnalyzer")
  add_subdirectory(StaticAnalyzer)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Format")
  add_subdirectory(Format)
endif()
if(CLANG_INCLUDE_TESTS)
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Testing")
  add_subdirectory(Testing)
endif()
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Interpreter")
  add_subdirectory(Interpreter)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Support")
  add_subdirectory(Support)
endif()

if(CLANG_ENABLE_CIR)
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/CIR")
  add_subdirectory(CIR)
endif()
endif()
