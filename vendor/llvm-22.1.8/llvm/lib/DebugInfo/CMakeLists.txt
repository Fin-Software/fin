if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/DWARF")
  add_subdirectory(DWARF)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/GSYM")
  add_subdirectory(GSYM)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/LogicalView")
  add_subdirectory(LogicalView)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/MSF")
  add_subdirectory(MSF)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/CodeView")
  add_subdirectory(CodeView)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/PDB")
  add_subdirectory(PDB)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/Symbolize")
  add_subdirectory(Symbolize)
endif()
if(EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/BTF")
  add_subdirectory(BTF)
endif()
