
////////////////////////////////////////////////////////////////////////////////
//
//    Fin_ParserTables.h (generated by LRSTAR 24.0.025)

#pragma once

#undef GRAMMAR
#undef PARSER
#undef PARSER_TABLES
#undef LEXER
#undef ACTIONS
#undef TERM_ACTIONS
#undef NODE_ACTIONS
#undef INSENSITIVE
#undef LOOKAHEADS
#undef DEBUG_PARSER
#undef DEBUG_TRACE
#undef MAKE_AST
#undef EXPECTING
#undef REVERSABLE
#undef SEMANTICS
#undef ND_PARSING
#undef ND_THREADS

#define GRAMMAR "Fin"
#define PARSER Fin_Parser
#define PARSER_TABLES Fin_ParserTables
#define LEXER Fin_Lexer
#define ACTIONS Fin_Actions
#define TERM_ACTIONS Fin_TermActions
#define LOOKAHEADS 2
#define EXPECTING
#define ND_PARSING
#define ND_THREADS 2

enum termcon {
    BOOL = 2,
    F16 = 3,
    BF16 = 4,
    F32 = 5,
    F64 = 6,
    I8 = 7,
    I16 = 8,
    INT = 9,
    I64 = 10,
    I128 = 11,
    WORD = 12,
    U8 = 13,
    CHAR = 14,
    U16 = 15,
    U32 = 16,
    RUNE = 17,
    U64 = 18,
    U128 = 19,
    UWORD = 20,
    ALIAS = 21,
    BANG = 22,
    CBRACE = 23,
    CBRACK = 24,
    COLON = 25,
    COMMA = 26,
    CPAREN = 27,
    CTID = 28,
    ELLIPSES = 29,
    EQUALS = 30,
    EXTEND = 31,
    FIN = 32,
    IMPL = 33,
    INLINE = 34,
    LINKABLE = 35,
    LINKED = 36,
    MODULE = 37,
    NAKED = 38,
    NORETURN = 39,
    OBRACE = 40,
    OBRACK = 41,
    OPAREN = 42,
    OPAREN_PCENT = 43,
    PCENT_CPAREN = 44,
    RTID = 45,
    SLASH = 46,
    STAR = 47,
    STRUCT = 48,
    QMARK = 49
};

typedef unsigned int uint;
typedef unsigned char uchar;
typedef unsigned short ushort;

#define TOP_DOWN 0
#define PASS_OVER 1
#define BOTTOM_UP 2

class Fin_ParserTables {
    friend class Fin_Parser;

  public:
    static char* term_symb[56];  // Terminal symbols of the grammar.
    static char* head_symb[42];  // Nonterminal symbols of the grammar.
    static char* tact_name[1];   // Terminal action names found in the grammar.
    static uchar head_numb[111]; // Head symbol number for a production.
    static uchar f_tail[112];    // First tail in a production.
    static char tail[191];       // Tail symbol number.
    static char arga[56];        // Arguments for terminal actions.

  private:
    static uchar Bm[68];  // Boolean matrix.
    static uchar Br[68];  // Boolean matrix row.
    static uchar Bc[56];  // Boolean matrix column.
    static uchar Bf[56];  // Boolean matrix filter/mask.
    static char Tm[94];   // Terminal transition matrix.
    static uchar Tr[68];  // Terminal transition matrix row.
    static uchar Tc[56];  // Terminal transition matrix column.
    static char Nm[112];  // Nonterminal transition matrix.
    static uchar Nr[68];  // Nonterminal transition matrix row.
    static uchar Nc[111]; // Nonterminal transition matrix column.
    static uchar Rm[67];  // Reduction matrix.
    static char Rr[68];   // Reduction matrix row.
    static uchar Rc[56];  // Reduction matrix column.
    static char PL[111];  // Production length minus one.

    static uchar nd_fterm[69];  // ND: first terminal in the list.
    static uchar nd_term[3];    // ND: terminal list.
    static uchar nd_faction[4]; // ND: first action in the list.
    static char nd_action[6];   // ND: action list.

    static char tact_numb[56]; // Terminal action numbers.

    static void (*init_func[2])();      // Init action function pointers.
    static int (*tact_func[1])(int& t); // Terminal action function pointers.
};

//
////////////////////////////////////////////////////////////////////////////////
