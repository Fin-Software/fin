
////////////////////////////////////////////////////////////////////////////////
//
//    Fin_ParserTables.h (generated by LRSTAR 24.0.025)

#pragma once

#undef GRAMMAR
#undef PARSER
#undef PARSER_TABLES
#undef LEXER
#undef ACTIONS
#undef TERM_ACTIONS
#undef NODE_ACTIONS
#undef INSENSITIVE
#undef LOOKAHEADS
#undef DEBUG_PARSER
#undef DEBUG_TRACE
#undef MAKE_AST
#undef EXPECTING
#undef REVERSABLE
#undef SEMANTICS
#undef ND_PARSING
#undef ND_THREADS

#define GRAMMAR "Fin"
#define PARSER Fin_Parser
#define PARSER_TABLES Fin_ParserTables
#define LEXER Fin_Lexer
#define ACTIONS Fin_Actions
#define TERM_ACTIONS Fin_TermActions
#define LOOKAHEADS 2
#define EXPECTING
#define ND_PARSING
#define ND_THREADS 2

enum termcon {
    BOOL = 2,
    F16 = 3,
    BF16 = 4,
    F32 = 5,
    F64 = 6,
    I8 = 7,
    I16 = 8,
    INT = 9,
    I64 = 10,
    I128 = 11,
    WORD = 12,
    U8 = 13,
    CHAR = 14,
    U16 = 15,
    U32 = 16,
    RUNE = 17,
    U64 = 18,
    U128 = 19,
    UWORD = 20,
    ALIAS = 21,
    BANG = 22,
    CBRACE = 23,
    CBRACK = 24,
    COLON = 25,
    COMMA = 26,
    CPAREN = 27,
    CTID = 28,
    ELLIPSES = 29,
    ENUM = 30,
    EQUALS = 31,
    EXTEND = 32,
    FIN = 33,
    IMPL = 34,
    INLINE = 35,
    LINKABLE = 36,
    LINKED = 37,
    MODULE = 38,
    NAKED = 39,
    NORETURN = 40,
    OBRACE = 41,
    OBRACK = 42,
    OPAREN = 43,
    OPAREN_PCENT = 44,
    PCENT_CPAREN = 45,
    POUND = 46,
    RTID = 47,
    SLASH = 48,
    STAR = 49,
    STRUCT = 50,
    QMARK = 51
};

typedef unsigned int uint;
typedef unsigned char uchar;
typedef unsigned short ushort;

#define TOP_DOWN 0
#define PASS_OVER 1
#define BOTTOM_UP 2

class Fin_ParserTables {
    friend class Fin_Parser;

  public:
    static char* term_symb[56];  // Terminal symbols of the grammar.
    static char* head_symb[48];  // Nonterminal symbols of the grammar.
    static char* tact_name[1];   // Terminal action names found in the grammar.
    static uchar head_numb[118]; // Head symbol number for a production.
    static uchar f_tail[119];    // First tail in a production.
    static char tail[208];       // Tail symbol number.
    static char arga[56];        // Arguments for terminal actions.

  private:
    static uchar Bm[92];  // Boolean matrix.
    static uchar Br[76];  // Boolean matrix row.
    static uchar Bc[56];  // Boolean matrix column.
    static uchar Bf[56];  // Boolean matrix filter/mask.
    static char Tm[112];  // Terminal transition matrix.
    static uchar Tr[76];  // Terminal transition matrix row.
    static uchar Tc[56];  // Terminal transition matrix column.
    static char Nm[123];  // Nonterminal transition matrix.
    static uchar Nr[76];  // Nonterminal transition matrix row.
    static uchar Nc[118]; // Nonterminal transition matrix column.
    static uchar Rm[67];  // Reduction matrix.
    static char Rr[76];   // Reduction matrix row.
    static uchar Rc[56];  // Reduction matrix column.
    static char PL[118];  // Production length minus one.

    static uchar nd_fterm[77];  // ND: first terminal in the list.
    static uchar nd_term[3];    // ND: terminal list.
    static uchar nd_faction[4]; // ND: first action in the list.
    static char nd_action[6];   // ND: action list.

    static char tact_numb[56]; // Terminal action numbers.

    static void (*init_func[2])();      // Init action function pointers.
    static int (*tact_func[1])(int& t); // Terminal action function pointers.
};

//
////////////////////////////////////////////////////////////////////////////////
